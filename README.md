in root directory:  
run npm install  
run npm run start  
  
Directions for branch 'backend' (now main but to check backend.) 
in root:  
run npm install  
run npm install -w express-backend express  
cd .\packages\express-backend\  
npm run dev  
y (to install nodemon)